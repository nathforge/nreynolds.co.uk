import{h as e,p as t,z as n}from"./t9v_umyn.js";import"./xihTtKlq.js";import"./CfUKv93C.js";var r={title:`Invert your .dockerignore for great profit`,date:`2025-11-12T00:00:00.000Z`,description:`Container registries hate this one weird trick.`},{title:i,date:a,description:o}=r,s=e(`<p>Here’s something I use on personal projects:</p> <p><code>.dockerignore:</code></p> <pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8"><code><span class="line"><span>*</span></span>
<span class="line"><span>!src</span></span>
<span class="line"><span>!package.json</span></span>
<span class="line"><span>!package-lock.json</span></span>
<span class="line"><span>!tsconfig.json</span></span></code></pre> <p>Notice that we ignore <em>everything</em> with <code>*</code>, then opt-in files via the <code>!</code> prefix.</p> <h2>Why?</h2> <p>Prepending lines with <code>!</code> makes .dockerignore files opt-in, not opt-out. You’re telling it what you want, not what you don’t want.</p> <p>With opt-out it’s easy to be forgetful. Does your <code>.dockerignore</code> contain <code>.git</code>? Have you added <code>node_modules</code>? <code>.env</code>? What could you have forgotten?</p> <p>Opt-in is:</p> <ol><li><strong>Easier to reason about</strong>. This is the main thing for me. We have tools like <a href="https://github.com/wagoodman/dive" rel="nofollow">dive</a> to explore image bloat, perhaps we even run it once in a while. Or… use opt-out and be explicit about what matters.</li> <li><strong>Faster builds</strong>: less content in your Docker context makes for faster builds.</li> <li><strong>Quicker deployments</strong> during build push and node pull.</li> <li><strong>Improved security</strong> - less chance of unexpected files appearing in your images.</li></ol> <p>Our tools don’t make it easy to manage Docker context bloat.<br/> It takes intentional effort that I’d prefer to avoid.</p>`,1);function c(e){var r=s();n(18),t(e,r)}export{c as default,r as metadata};